# Example web configuration file for ollama_exporter.
# Pass with: --web.config.file=web.config.yml
#
# Uncomment sections as needed. All fields are optional.

# tls_server_config:
#   cert_file: /etc/ollama-exporter/tls.crt
#   key_file:  /etc/ollama-exporter/tls.key

# http_server_config:
#   http2: true

# basic_auth_users:
#   # Generate password hash: htpasswd -nBC 12 '' | tr -d ':\n'
#   prometheus: $2y$12$YourBcryptHashHere
