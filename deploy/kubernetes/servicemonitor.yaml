apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ollama-exporter
  labels:
    app: ollama-exporter
spec:
  selector:
    matchLabels:
      app: ollama-exporter
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
